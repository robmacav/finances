import 'package:flutter/material.dart';

class NewPageContent extends StatelessWidget {
  const NewPageContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Cadastro de Transações', style: TextStyle(fontSize: 24)));
  }
}
